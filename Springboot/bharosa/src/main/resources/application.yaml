spring:
  application:
    name: bharosa

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
   

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8080

# Database connectivity configuration

# Task
# Dependency, CRUD operations, userControllers (4 API/Endpoints), 4 Service Methods, 4 repositories (.save()/.saveAll())
# Pagination

# Java 17, Springboot extension pack from vmware

# 